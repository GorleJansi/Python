n=[1,2,3,4,5]
for i in reversed(n):
    print(i)



# reversed() is a built-in Python function.
# It does not create a new reversed list (to save memory).
# Instead, it returns a reverse iterator → something you can loop through from last element to first.   